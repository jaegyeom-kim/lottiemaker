# 체인지로그

LottieMaker의 주요 변경 이력. 최신 날짜가 위.

## 2026-08-13

### 기능
- **패스 애니메이션** — 펜/도형 패스에 키프레임을 찍어 형태 모핑. Properties 토글로 켜고 끄기, 펜 편집 시 재생헤드에 자동 키(AE 방식), 타임라인 '패스' 채널 행 + ◆ 스냅샷, 구간별 이징 지원 (`d9d3903`)
- **도형 옵션 패널** — 사각형·원형·삼각형·별 레이어에 W/H 지오메트리 입력, 사각형 라운드 코너 (`f75b0cc`)
- **펜 포인트 다중선택** — ⇧클릭, 그룹 드래그, 마키 선택 (AE 방식) (`34deafd`)
- **⇧마키 토글** — 선택된 포인트 위로 ⇧드래그하면 해제, 새 포인트는 추가 (일러 방식) (`0ca2047`)
- **⌥클릭 포인트 변환 토글** — 코너 ↔ 스무스. 코너→스무스 시 살짝 당겨진 핸들 생성 (0길이 핸들 못 잡는 문제 해결) (`2d2fc1b`)
- **펜 편집 컨텍스트 커서** — 앵커 호버(near_me 반전), 베지어 핸들(양쪽/⌥한쪽 구분 글리프), ⌥ 변환(커브) (`1246e9e`~)

### 수정
- 펜 첫 앵커가 기존 바운딩박스 밖으로 자유롭게 드래그 안 되던 문제 (`aa339f8`)
- 펜 모드에서 레이어 앵커 마커(⊕) 숨김 — 패스 앵커와 혼동 방지 (`272d8d5`)
- ⌥ 변환 커서 핫스팟을 글리프 팁으로 — 포인트에 정확히 닿게 (`7023069`)
- 빈 키프레임 정리가 트림(ts/te) 채널을 무시하던 잠재 버그 (`d9d3903`에 포함)

### 성능
- **오버레이 드래그 아키텍처** — 레이어 이동 드래그가 틱마다 lottie를 재구축하지 않고 렌더된 `<g>`를 직접 이동. 재구축 28회→2회, 체감 38→119fps (`51a1a14`)

### 스타일
- 펜 커서 크기 통일 — 18px 캔버스, 시각 크기 실측 기준 정합 (`8e94cba`, `ec7434c`, `e92e748`)
- 사용자 제공 커브 글리프로 베지어/변환 커서 교체 (`cece46a`, `3e4092a`, `06a0fde`)

## 2026-08-12

### 기능
- **앵커 포인트 툴 (Y)** — 바운딩박스에 앵커 마커 표시, 드래그로 앵커만 이동 (그래픽 제자리, 스케일·회전·프레임 무관 동작) (`55a6502`, `addd6a2`)
- **펜 패스 편집 모드** — 완성된 패스의 포인트/베지어 핸들 수정, 선(스트로크) 두께·라인캡 옵션 (일러 방식) (`670ea0a`, `693353f`)
- **Properties 개편** — P/S/R/O 숫자 입력 (슬라이더 제거), 키 있는 채널만 재생헤드 자동 키 (AE 스톱워치 시맨틱) (`d286f20`, `2346d3b`)
- **휠버튼 팬** — 마우스 가운데 버튼 드래그 = 핸드 툴 (`693353f`)
- **레이어 잠금(xlock) store 차원 강제** — 모든 뮤테이터에서 차단 (`96d7efe`)
- **AI API 키 저장 시 검증** (`368d996`)
- **WebM 비디오 내보내기** — lottie-web + MediaRecorder (`9928155`)

### 수정
- **보정 누락 버그 사냥 (3차)** — 스케일·회전·앵커·프레임 가정을 깔고 있던 지오메트리 코드 20여 곳 수정. `layerScaleOf`/`layerRotationOf`/`layerAabbOf`/`layerBaseOf` 단일 소스화 (`52a0909`, `ae61c40`, `495cdb1`, `8a98d44`)
- 펜 베지어 핸들 당길 때 고스트와 실제 렌더가 어긋나던 문제 — 곡선 극값 bbox로 정합 0.00px (`206f1d0`)
- 펜 편집 후 바운딩박스가 새 지오메트리를 따라오지 않던 문제 (`a8fffe4`)
- 펜 undo/redo 시맨틱 (그리는 중 ⌘Z = 마지막 점 취소, 스트로크 = 언두 1회) + 캔버스 밖 레이어 선택 (`6facfe1`)
- 타임라인 재생헤드가 키프레임 클릭을 삼키던 문제 (`1cf00b3`)
- 휠버튼 누를 때 레이어가 선택·드래그되던 오류 (`bee0ca1`)
- 임포트 병합·패턴 복제 시 매트/부모 참조 유지 (`c28c7f6`)

### 스타일/기타
- 가이드 표시 일관성 — 캔버스/타임라인 전체를 하나의 액센트 시스템으로 통일 (`f7a731f`)
- 앵커 마커 시인성 — Material `my_location` 크로스헤어 (`76c6d2e`)
- TypeScript strict 모드 (`50ac144`)
- 렌더러별 lottie 빌드로 초기 번들 축소 (`89c9f65`)
- 죽은 CSS 제거·중복 블록 병합 (`62f678f`)
- **레포 기반 E2E 스위트** — dev 서버 자동 기동 러너 + headless 테스트 (`16e370d`)

## 2026-08-03

- **AI 모션 생성** — 프롬프트로 레이어 모션 키프레임 생성, 이즈 토큰 관리 (`f24152c`)

## 2026-07-20

- **키프레임 모드** — 레이어 편집용 키프레임 시스템 도입 (`140d6b9`)

## 2026-07-16

- 레이어 정렬/분배 패널 (`7c56c5a`)
- 테마 전환 토스트 (`f768bba`)
- README 전면 개정 (`ae05df4`)

## 2026-07-14

- 세션 관리 + 자동 저장 (`de8516a`)

## 2026-07-13

- 웨이브 바 애니메이션 barCount 노브 (`968b83b`)

## 2026-07-09

- 최초 커밋 — 템플릿 + 스왑 슬롯 (`b870ab4`, `bb3a8a8`)
